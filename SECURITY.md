# 🔒 Документация по безопасности

## Обзор мер безопасности

Это приложение реализует множество мер безопасности для защиты пользовательских данных.

---

## 1. Хеширование паролей

### Алгоритм: PBKDF2-SHA256

**Параметры:**
- **Алгоритм**: PBKDF2 (Password-Based Key Derivation Function 2)
- **Хеш-функция**: SHA-256
- **Соль**: 16 байт (случайная для каждого пароля)
- **Итерации**: По умолчанию Werkzeug (достаточно для защиты)

**Почему это безопасно:**
- ✅ Каждый пароль имеет уникальную соль
- ✅ Защита от rainbow table атак
- ✅ Множественные итерации замедляют брутфорс
- ✅ Оригинальный пароль никогда не сохраняется
- ✅ Невозможно восстановить пароль из хеша

**Код:**
```python
def set_password(self, password):
    self.password_hash = generate_password_hash(
        password,
        method='pbkdf2:sha256',
        salt_length=16
    )
```

---

## 2. Защита сессий

### Настройки Cookie

**SESSION_COOKIE_SECURE = True** (продакшн)
- Cookie передаются только через HTTPS
- Защита от перехвата трафика

**SESSION_COOKIE_HTTPONLY = True**
- Cookie недоступны через JavaScript
- Защита от XSS атак

**SESSION_COOKIE_SAMESITE = 'Lax'**
- Cookie не отправляются с запросами от других сайтов
- Защита от CSRF атак

**PERMANENT_SESSION_LIFETIME = 1 час**
- Автоматическое истечение сессии
- Снижение риска при краже сессии

---

## 3. CSRF защита

### Flask-WTF CSRF Tokens

**Что это:**
- Уникальный токен для каждой формы
- Проверяется на сервере при отправке

**Как работает:**
1. Сервер генерирует токен при отображении формы
2. Токен включается в скрытое поле формы
3. При отправке сервер проверяет валидность токена
4. Запросы без токена или с неверным токеном отклоняются

**Код в формах:**
```python
{{ form.hidden_tag() }}  # Автоматически добавляет CSRF токен
```

---

## 4. Валидация данных

### Серверная валидация (WTForms)

**Username:**
- ✅ 3-80 символов
- ✅ Только латиница, цифры, дефис, подчеркивание
- ✅ Проверка уникальности

**Email:**
- ✅ Валидация формата email
- ✅ Проверка уникальности

**Password:**
- ✅ Минимум 8 символов
- ✅ Обязательные заглавные буквы
- ✅ Обязательные строчные буквы
- ✅ Обязательные цифры
- ✅ Подтверждение пароля

**Код:**
```python
Regexp(
    r'^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)',
    message='Пароль должен содержать заглавные и строчные буквы, цифры'
)
```

---

## 5. Защита от SQL инъекций

### SQLAlchemy ORM

**Что это:**
- Object-Relational Mapping
- Автоматическое экранирование параметров

**Почему безопасно:**
- ✅ Параметризованные запросы
- ✅ Автоматическое экранирование
- ✅ Нет прямого SQL в коде

**Пример безопасного запроса:**
```python
user = User.query.filter_by(username=username).first()
# SQLAlchemy автоматически экранирует username
```

---

## 6. Защита от XSS

### Автоматическое экранирование (Jinja2)

**Что это:**
- Jinja2 автоматически экранирует HTML
- Предотвращает внедрение скриптов

**Пример:**
```html
{{ user.username }}
<!-- Автоматически экранируется -->
```

---

## 7. Защита от Brute Force

### Рекомендации для продакшена:

**Реализуйте rate limiting:**
- Flask-Limiter для ограничения попыток входа
- Капча после нескольких неудачных попыток
- Временная блокировка после многих неудач

**Пример (нужно добавить):**
```python
from flask_limiter import Limiter

limiter = Limiter(
    app,
    key_func=get_remote_address,
    default_limits=["200 per day", "50 per hour"]
)

@app.route('/login', methods=['POST'])
@limiter.limit("5 per minute")
def login():
    # ...
```

---

## 8. Безопасное хранение конфигурации

### Переменные окружения (.env)

**Что хранится в .env:**
- SECRET_KEY
- DATABASE_URL
- Другие чувствительные данные

**Важно:**
- ✅ .env в .gitignore
- ✅ Никогда не коммитить секреты
- ✅ Разные ключи для development/production

---

## 9. Безопасность базы данных

### SQLite (для разработки)

**Ограничения:**
- Только для разработки и тестирования
- Не подходит для продакшена с высокой нагрузкой

### PostgreSQL (для продакшена)

**Рекомендации:**
- Используйте PostgreSQL в продакшене
- Настройте SSL подключение
- Регулярные резервные копии
- Ограниченные права пользователя БД

---

## 10. HTTPS

### Обязательно для продакшена!

**Что защищает:**
- Шифрование передачи данных
- Защита от перехвата паролей
- Защита от MITM атак

**Как настроить:**
1. Получите SSL сертификат (Let's Encrypt - бесплатно)
2. Настройте Nginx/Apache как reverse proxy
3. Включите `SESSION_COOKIE_SECURE = True`

---

## Чек-лист для продакшена

- [ ] Используйте PostgreSQL вместо SQLite
- [ ] Включите HTTPS
- [ ] Установите `SESSION_COOKIE_SECURE = True`
- [ ] Смените `SECRET_KEY` на сложный уникальный
- [ ] Отключите DEBUG режим
- [ ] Добавьте rate limiting
- [ ] Настройте логирование
- [ ] Регулярные бэкапы БД
- [ ] Мониторинг безопасности
- [ ] Регулярные обновления зависимостей
- [ ] Настройте firewall
- [ ] Добавьте мониторинг неудачных попыток входа
- [ ] Реализуйте двухфакторную аутентификацию (опционально)
- [ ] Добавьте восстановление пароля через email

---

## Известные ограничения

### Текущая версия НЕ включает:

- ❌ Rate limiting для защиты от брутфорса
- ❌ Двухфакторная аутентификация
- ❌ Восстановление пароля
- ❌ Email верификация
- ❌ История входов
- ❌ Блокировка подозрительной активности

**Эти функции нужно добавить перед использованием в продакшене!**

---

## Тестирование безопасности

### Инструменты для тестирования:

1. **OWASP ZAP** - сканер безопасности веб-приложений
2. **Bandit** - сканер безопасности Python кода
3. **Safety** - проверка уязвимостей в зависимостях

**Запуск проверки зависимостей:**
```bash
pip install safety
safety check -r requirements.txt
```

---

## Отчет об уязвимостях

Если вы обнаружили уязвимость безопасности, пожалуйста:

1. **НЕ** создавайте публичный issue
2. Сообщите напрямую разработчику
3. Дайте время на исправление перед публикацией

---

## Ресурсы по безопасности

- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [Flask Security Best Practices](https://flask.palletsprojects.com/en/latest/security/)
- [NIST Password Guidelines](https://pages.nist.gov/800-63-3/)
- [SQLAlchemy Security](https://docs.sqlalchemy.org/en/latest/core/security.html)

---

**⚠️ Дисклеймер**: Это приложение создано в образовательных целях. Перед использованием в продакшене проведите полный аудит безопасности и реализуйте дополнительные меры защиты согласно требованиям вашего проекта.

