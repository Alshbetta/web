# 🚀 Быстрый старт

## Автоматическая установка (рекомендуется)

### macOS/Linux:

```bash
# 1. Запустите установку
./setup.sh

# 2. Запустите приложение
./start.sh
```

Приложение будет доступно по адресу: **http://localhost:5000**

---

## Ручная установка

Если автоматическая установка не работает:

### 1. Создайте виртуальное окружение

```bash
python3 -m venv venv
```

### 2. Активируйте его

**macOS/Linux:**
```bash
source venv/bin/activate
```

**Windows:**
```bash
venv\Scripts\activate
```

### 3. Установите зависимости

```bash
pip install -r requirements.txt
```

### 4. Создайте файл `.env`

```bash
# Сгенерируйте секретный ключ
python3 -c "import secrets; print(secrets.token_hex(32))"

# Создайте файл .env и добавьте:
# SECRET_KEY=ваш-сгенерированный-ключ
# DATABASE_URL=sqlite:///users.db
# FLASK_ENV=development
# FLASK_DEBUG=True
```

### 5. Запустите приложение

```bash
python run.py
```

---

## Первый запуск

1. Откройте браузер: **http://localhost:5000**
2. Нажмите **"Зарегистрироваться"**
3. Заполните форму:
   - **Username**: любое имя (латиницей)
   - **Email**: действительный email
   - **Пароль**: минимум 8 символов, заглавные и строчные буквы, цифры
   - Пример: `MyPass123`
4. Нажмите **"Зарегистрироваться"**
5. Войдите с созданными учетными данными

---

## Тестовый пользователь

Для быстрого тестирования создайте пользователя:

- **Username**: `admin`
- **Email**: `admin@example.com`
- **Password**: `Admin123`

---

## Остановка приложения

Нажмите `Ctrl+C` в терминале где запущено приложение.

---

## Проблемы?

Смотрите подробную документацию в **README.md**

### Частые проблемы:

**Ошибка порта занят:**
```bash
# Измените порт в run.py на другой (например, 8080)
```

**Ошибка SECRET_KEY:**
```bash
# Создайте файл .env с секретным ключом
python3 -c "import secrets; print('SECRET_KEY=' + secrets.token_hex(32))" > .env
```

**Ошибки импорта:**
```bash
# Убедитесь что виртуальное окружение активировано
source venv/bin/activate  # macOS/Linux
pip install -r requirements.txt
```

