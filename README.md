# 🔐 SecureApp - Безопасное Веб-Приложение

Современное веб-приложение на Python Flask с безопасным хранением учетных данных пользователей.

## 🌟 Особенности

### Безопасность
- ✅ **Безопасное хеширование паролей**: PBKDF2-SHA256 с 16-байтовой солью
- ✅ **Защита сессий**: HttpOnly, Secure, SameSite cookies
- ✅ **CSRF защита**: Flask-WTF токены для всех форм
- ✅ **Защита от SQL инъекций**: SQLAlchemy ORM
- ✅ **Валидация данных**: Серверная валидация с WTForms
- ✅ **Безопасные настройки**: Конфигурация для development и production

### Функциональность
- 👤 Регистрация пользователей
- 🔑 Аутентификация и авторизация
- 📊 Личный кабинет пользователя
- 👁️ Просмотр профиля
- 🕐 Отслеживание времени входа
- 🎨 Современный адаптивный UI

## 🛠️ Технологии

- **Backend**: Flask 3.0
- **Database**: SQLAlchemy + SQLite
- **Authentication**: Flask-Login
- **Forms**: Flask-WTF
- **Security**: Werkzeug, cryptography
- **Frontend**: HTML5, CSS3, Vanilla JavaScript

## 📋 Требования

- Python 3.8+
- pip (менеджер пакетов Python)

## 🚀 Установка и запуск

### 1. Клонирование репозитория

```bash
cd /путь/к/проекту
```

### 2. Создание виртуального окружения

```bash
python3 -m venv venv
```

### 3. Активация виртуального окружения

**macOS/Linux:**
```bash
source venv/bin/activate
```

**Windows:**
```bash
venv\Scripts\activate
```

### 4. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 5. Настройка переменных окружения

Создайте файл `.env` в корне проекта:

```env
SECRET_KEY=ваш-уникальный-секретный-ключ-измените-это
DATABASE_URL=sqlite:///users.db
FLASK_ENV=development
FLASK_DEBUG=True
```

**Важно!** Сгенерируйте уникальный секретный ключ:

```python
python3 -c "import secrets; print(secrets.token_hex(32))"
```

### 6. Запуск приложения

```bash
python app.py
```

Приложение будет доступно по адресу: `http://localhost:5000`

## 📁 Структура проекта

```
web/
├── app.py                 # Главный файл приложения
├── config.py             # Конфигурация приложения
├── models.py             # Модели базы данных
├── forms.py              # Формы с валидацией
├── requirements.txt      # Зависимости
├── .gitignore           # Игнорируемые файлы
├── README.md            # Документация
├── static/              # Статические файлы
│   ├── css/
│   │   └── style.css    # Стили
│   └── js/              # JavaScript файлы
└── templates/           # HTML шаблоны
    ├── base.html        # Базовый шаблон
    ├── index.html       # Главная страница
    ├── register.html    # Регистрация
    ├── login.html       # Вход
    ├── dashboard.html   # Панель управления
    ├── profile.html     # Профиль
    └── errors/          # Страницы ошибок
        ├── 404.html
        └── 500.html
```

## 🔒 Механизмы безопасности

### 1. Хеширование паролей

Пароли хешируются с использованием **PBKDF2-SHA256**:
- 16-байтовая случайная соль для каждого пароля
- Множество итераций (по умолчанию в Werkzeug)
- Оригинальный пароль никогда не хранится

```python
# models.py
def set_password(self, password):
    self.password_hash = generate_password_hash(
        password,
        method='pbkdf2:sha256',
        salt_length=16
    )
```

### 2. Защита сессий

```python
# config.py
SESSION_COOKIE_SECURE = True      # Только HTTPS (в продакшене)
SESSION_COOKIE_HTTPONLY = True    # Защита от XSS
SESSION_COOKIE_SAMESITE = 'Lax'   # Защита от CSRF
```

### 3. CSRF защита

Все формы защищены CSRF токенами:

```python
# forms.py
class RegistrationForm(FlaskForm):
    # Flask-WTF автоматически добавляет CSRF защиту
    ...
```

### 4. Валидация данных

Строгая валидация на сервере:
- Минимальная длина пароля: 8 символов
- Обязательные заглавные и строчные буквы, цифры
- Валидация email
- Уникальность username и email
- Защита от SQL инъекций через ORM

## 📱 API Endpoints

| Метод | URL | Описание | Аутентификация |
|-------|-----|----------|----------------|
| GET | `/` | Главная страница | Нет |
| GET/POST | `/register` | Регистрация | Нет |
| GET/POST | `/login` | Вход | Нет |
| GET | `/logout` | Выход | Да |
| GET | `/dashboard` | Панель управления | Да |
| GET | `/profile` | Профиль пользователя | Да |

## 🧪 Тестирование

### Создание тестового пользователя

1. Запустите приложение
2. Перейдите на `/register`
3. Заполните форму:
   - Username: `testuser`
   - Email: `test@example.com`
   - Password: `TestPass123` (минимум 8 символов, буквы разного регистра, цифры)
4. Войдите через `/login`

## 🚀 Деплой в продакшен

### Важные шаги для продакшена:

1. **Измените SECRET_KEY** на уникальный и сложный
2. **Используйте PostgreSQL** вместо SQLite:
   ```env
   DATABASE_URL=postgresql://user:password@localhost/dbname
   ```
3. **Отключите DEBUG**:
   ```env
   FLASK_ENV=production
   FLASK_DEBUG=False
   ```
4. **Используйте HTTPS** (например, с Let's Encrypt)
5. **Используйте WSGI сервер** (Gunicorn, uWSGI):
   ```bash
   pip install gunicorn
   gunicorn app:app -b 0.0.0.0:5000
   ```
6. **Настройте обратный прокси** (Nginx, Apache)
7. **Включите логирование**
8. **Регулярно обновляйте зависимости**

### Пример конфигурации Gunicorn

```bash
gunicorn --workers 4 --bind 0.0.0.0:5000 --timeout 120 app:app
```

## 🔧 Настройка

### Изменение порта

В файле `app.py`:

```python
app.run(host='0.0.0.0', port=8080)  # Измените порт
```

### Добавление новых полей в User

1. Обновите модель в `models.py`
2. Создайте миграцию (с Flask-Migrate) или пересоздайте БД
3. Обновите формы в `forms.py`
4. Обновите шаблоны

## 📝 Возможные улучшения

- [ ] Восстановление пароля через email
- [ ] Двухфакторная аутентификация (2FA)
- [ ] OAuth (Google, GitHub)
- [ ] Rate limiting для защиты от брутфорса
- [ ] Логирование попыток входа
- [ ] Миграции БД с Flask-Migrate
- [ ] API endpoints с JWT
- [ ] Админ-панель
- [ ] Смена пароля
- [ ] Удаление аккаунта

## 🐛 Решение проблем

### База данных не создается

```bash
python
>>> from app import create_app, db
>>> app = create_app()
>>> with app.app_context():
...     db.create_all()
```

### Ошибка импорта

Убедитесь, что виртуальное окружение активировано и зависимости установлены:

```bash
pip install -r requirements.txt
```

### Проблемы с SECRET_KEY

Сгенерируйте новый ключ:

```python
python3 -c "import secrets; print(secrets.token_hex(32))"
```

## 📄 Лицензия

MIT License - свободное использование для личных и коммерческих проектов.

## 👨‍💻 Автор

Создано как демонстрация безопасного веб-приложения на Python Flask.

## 🤝 Вклад

Предложения и улучшения приветствуются! Создайте Issue или Pull Request.

---

**⚠️ Важно**: Это приложение создано в образовательных целях. Перед использованием в продакшене убедитесь в соответствии всем требованиям безопасности вашего проекта.

